# Copy to ".env" and adjust. Do not commit ".env".

# Strong random secret for sessions and CSRF (use a long random string in production).
# SECRET_KEY=

# Optional override (default: sqlite:///database.db relative to cwd).
# DATABASE_URL=sqlite:///database.db
